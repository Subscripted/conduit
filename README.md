Conduit is an open-source project that enables the implementation of various LLM REST APIs in PHP web applications. Its goal is to provide a simple, standardized structure for calling different AIs. The call is always the same, regardless of the API used. If a different model is required, the model parameter can be easily replaced with the desired model's. This provides, if implemented, a simple failover mechanism.
